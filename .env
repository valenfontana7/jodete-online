# Este archivo queda como referencia segura.
# Colocá tus valores reales en `.env.local` (ya ignorado por git).
# También podés copiar `.env.example` y renombrarlo a `.env.local`.

# PostgreSQL Local
DB_HOST=localhost
DB_PORT=5432
DB_NAME=jodete_online
DB_USER=postgres
DB_PASSWORD=tu_password_segura
DATABASE_URL=postgresql://postgres:tu_password_segura@localhost:5432/jodete_online

# Google OAuth (reemplazá por tus credenciales)
GOOGLE_CLIENT_ID=tu_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=tu_client_secret

# JWT Secret (generá uno aleatorio en cada entorno)
JWT_SECRET=poné_un_valor_seguro_aquí

# URLs
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3001

# Node
NODE_ENV=development