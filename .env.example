# Puerto del servidor (HTTP + Socket.IO)
PORT=3001

# Orígenes permitidos para CORS (separados por coma, opcional)
# Ejemplo: CLIENT_ORIGINS=https://miapp.com,https://otro.com
# Si no se configura, permite todos los orígenes (*)
# CLIENT_ORIGINS=

# Entorno de ejecución (opcional)
# NODE_ENV=production

# URL del socket para el cliente (opcional, autodetecta por defecto)
# Ejemplo: VITE_SOCKET_URL=https://miservidor.com
# VITE_SOCKET_URL=

# ===== PostgreSQL =====
# Opción 1: URL completa (recomendado para producción)
# DATABASE_URL=postgresql://usuario:password@host:5432/nombre_db

# Opción 2: Variables individuales (para desarrollo local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=jodete_online
DB_USER=postgres
DB_PASSWORD=tu_password_aqui

# ===== Google OAuth 2.0 =====
GOOGLE_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_client_secret_here

# ===== JWT =====
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu_secreto_jwt_super_seguro_aqui

# ===== URLs =====
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://localhost:3001
